name: Release & Publish

on:
  push:
    branches:
      - release

permissions:
  contents: write
  packages: write
  id-token: write

jobs:
  release:
    name: Release
    uses: ./.github/workflows/_release.yml
    with:
      package-name: opencode-context-watch
      publish-npm: true
    secrets: inherit
