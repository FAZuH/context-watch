name: Release & Publish

on:
  push:
    branches:
      - release

permissions:
  contents: write
  packages: write

jobs:
  release:
    name: Release
    uses: ./.github/workflows/_release.yml
    with:
      package-name: opencode-context-watch
      publish-npm: false
    secrets: inherit
